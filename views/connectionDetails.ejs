<!doctype html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Round Table</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" />
	<link rel="stylesheet" href="/style.css" type="text/css"></link>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

</head>

<body>
    <%- include('navBar.ejs') -%>
    <% if(flag==1){  %>
        <script > M.toast({html: "Same response !!!",classes: 'rounded'});</script>
        <%}%>
    <div class="container">
     <h3><%=connectionItem.title %></h3>
        </br>
        <div class="row">
            <div class="col s2">
                <img id="dp" src="/logo.jpg" alt="" class="materialboxed" />

            </div>
            <div class="col s5">
                <h5>Created By-<%= connectionItem.name%></h5>
                <p><%=connectionItem.date%></p>
                <p><%=connectionItem.timeFrom%> -<%=connectionItem.timeTo%></p>
                <p><%=connectionItem.address%></p>
            </div>

            <div class="col s3">
             
                <% if(connectionItem.userId!=id ) { %>
                    <p id="detailText">Are you going ? <%=connectionDetail.upVoted%>  people are going</p>
                <a id="detailButton" class="btn waves-effect waves-light"
                    href="/connections/savedConnections/Yes/<%=connectionItem.title %>/<%=connectionItem.category %>/<%=connectionDetail.conId%>">Yes</a>
                <a id="detailButton" class="btn waves-effect waves-light red"
                href="/connections/savedConnections/No/<%=connectionItem.title %>/<%=connectionItem.category %>/<%=connectionDetail.conId%>">No</a>
                <a id="detailButton" class="btn waves-effect waves-light blue" 
                href="/connections/savedConnections/Maybe/<%=connectionItem.title %>/<%=connectionItem.category %>/<%=connectionDetail.conId%>">Maybe</a>
              <%} else {%>
                <p id="detailText"><%=connectionDetail.upVoted%>  people are going</p>
                <%}%>
            </div>
        </div>
        <h3>Description</h3>
                <p><%=connectionItem.description%></p>
        </br>

    </div>




    <span style="position: fixed; bottom: 0;	width: 100%"><%- include('fotter.ejs') -%></span>	</body>
</body>

</html>